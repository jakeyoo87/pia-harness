# pia-harness

사용자와 함께 확정한 Harness 설계 흐름을 기록한다.

## 1. Harness Flow Overview

```text
사용자 요청
    ↓
Context 구성 (→ 3, 4)
(대화 기록·Memory·이번 요청·이번에 얻은 도구 결과)
    ↓
◇ Jev가 next_action 하나를 선택
  │
  ├─ 답변
  │    · answer + MemoryAction (NONE / UPDATE / FORGET) (→ 5)
  │        · 일반 질문에 답하기
  │        · 부족한 조건을 사용자에게 질문하기
  │        · 도구로 확인한 결과를 설명하기
  │        → 같은 Jev 응답에서 이번 요청의 MemoryAction도 결정
  │        → LLM이 이번 Turn에서 확보한 자료로 답변 작성
  │        → 필요한 Memory 변경 처리
  │        → 사용자에게 전달 → Turn 저장 (→ 3) → 종료
  │
  ├─ 읽기 도구 (→ 6.1)
  │    · 6.1.1 Search
  │        · 뉴스 기사 후보 찾기
  │        → LLM이 검색어 작성
  │    · 6.1.2 본문 읽기 (read:후보ID)
  │        · 선택한 기사·URL 본문 읽기
  │    · 그 밖의 도구 ID는 등록할 때 결정
  │        · 시세·잔고·주문 상태 조회
  │        · 공시 등 다른 자료 조회
  │    → Harness가 해당 도구에 직접 요청 ────────────┐
  │                                                │
  ├─ 코딩 도구 (미구현)                            │
  │    · 도구 ID는 등록할 때 결정                    │
  │        · 자료 정렬·계산·분석                     │
  │        · 요청에 맞는 일회성 코드 작성·격리 실행    │
  │    → LLM이 코드 작성                             │
  │    → Harness가 실행 환경에 직접 요청 ───────────┤
  │                                                │
  └─ 실행 도구 (미구현)                            │
       · 도구 ID는 등록할 때 결정                    │
           · 매수·매도 주문                         │
           · 주문 취소 등 상태를 바꾸는 작업         │
       → Harness가 이번 실행권 확정 (→ 2)           │
       → pia-broker에 직접 요청                     │
       → Broker가 검증·처리한 결과를 받음 ──────────┘
                                                   ↓
                                          결과를 Context에 추가
                                                   ↓
                                          Context 다시 구성
                                                   ↓
                                          Jev가 다시 선택
```

**상세 사양**

- [2. 처리 중 새 메시지](#2-같은-사용자가-처리-중-새-메시지를-보낼-때): 실행권 확정 전후의 취소 규칙
- [3. Context 구성과 Turn 연속성](#3-context-구성과-turn-연속성)
- [4. Compaction과 대화 보존](#4-compaction과-대화-보존)
- [5. 장기 Memory 관리](#5-장기-memory-관리): MemoryAction
- [6.1 읽기 도구](#61-읽기-도구): 공통 규칙
  - [6.1.1 Search](#611-search)
  - [6.1.2 본문 읽기](#612-본문-읽기)

**구현 현황**: 현재 구현된 분기는 답변과 읽기 도구다. 코딩 도구와 실행 도구는 목표 설계다. 실행 도구는 Harness가 이번 실행권을 확정한 뒤에만 실행해야 한다. 지금의 읽기 도구는 실행권 확정 전에 실행되므로, 주문·취소처럼 상태를 바꾸는 작업을 읽기 도구로 등록하지 않는다.

**답변만 종료 지점**이다. 읽기·코딩·실행 도구의 결과는 모두 이번 Context에 추가하고 Jev가 다음 행동을 다시 선택한다. 네 분류는 이해를 돕는 묶음이며, `next_action`의 실제 값은 `answer`, 현재 등록된 도구 ID, 또는 아직 읽지 않은 `read:후보ID`다. `MemoryAction`은 `answer`를 선택한 같은 Jev 응답에서만 적용하며, 도구를 선택한 동안에는 `NONE`으로 미룬다. Jev는 이번 요청의 사용자 메시지(처리 중 합쳐진 메시지 포함)와 이번 Turn의 도구 요청·결과만 보고 판단한다. 대화 기록·Memory·요약은 답변 LLM에만 전달한다.

Harness는 도구에 직접 요청하고 결과를 받아 다음 판단에 연결한다. PIA는 도구 요청을 매번 중계하지 않고 사용자 요청의 채널 입구와 최종 답변 전달을 맡는다. 주문 조건의 최종 검증과 처리는 pia-broker가 맡는다. **실행권 확정은 Harness 내부에서 이번 작업을 더 이상 새 메시지로 취소하지 않기로 하는 단계**이며, 사용자에게 추가 승인을 요청한다는 뜻이 아니다. 읽기 도구 사용에는 임의의 고정 호출 횟수를 두지 않되, 한 요청의 읽기 라우팅 시간은 기본 120초로 제한한다. 시간을 넘기면 이번 입력을 종료하고 도구를 자동 재실행하지 않는다.

## 2. 같은 사용자가 처리 중 새 메시지를 보낼 때

```text
같은 사용자의 이전 메시지 처리 중
                ↓
          새 메시지 도착
                ↓
        ◇ 현재 처리 단계? — 코드의 if문
          ├─ 답변 생성 중
          │    → 이전 생성 취소
          │    → 미완료 입력과 새 입력을 합쳐 다시 생성
          │
          └─ 실행 확정 후
               → 진행 중인 작업은 마침
               → 새 메시지는 다음 작업으로 대기
```

처리 중인 작업이 없으면 바로 새 답변 생성을 시작한다. 한 사용자에게 진행 중인 답변 생성은 한 번에 하나이며, 다른 사용자들의 작업과는 별도로 관리한다. 단계 확인은 LLM이 아닌 Harness 코드가 한다.

## 3. Context 구성과 Turn 연속성

```text
새 사용자 요청
      ↓
Context 구성
  System 지침
  Memory
  Summary
  이전 완료 Turns  (사용자 요청 + 도구 요청·결과 + 최종 답변)
  이번 사용자 요청 + 이번 Turn의 도구 결과(있다면)
      ↓
◇ 모델 호출 직전 입력 크기 확인 — 코드의 if문
  ├─ 충분함 → 모델 호출
  └─ 너무 큼 → 4. Compaction 진행
               → Context 다시 구성 → 크기 재확인
      ↓
모델 판단
  ├─ 읽기 도구 요청 → 결과를 이번 Context에 추가
  │                  → Context 구성으로 돌아감
  └─ 최종 답변 → 이번 Turn 전체 저장
                  → 다음 요청의 '이전 완료 Turns'에서 읽음
```

System 지침은 host가 제공하는 규칙이다. Memory는 오래 기억할 사용자 정보, Summary는 오래된 대화의 요약이며, 둘 다 없으면 Context에서 생략한다. 이전 Turns·이번 요청·도구 결과도 모델이 참고할 데이터이지 System 지침이 아니다.

**입력 예산은 모델의 전체 Context 한도에서 답변용 예약을 뺀 값**이다. 크기 판정은 코드가 하고, Summary 생성에 LLM을 사용한다. 최종 답변 뒤 별도 Compaction 검사는 하지 않고 다음 모델 호출 직전에 확인한다.

## 4. Compaction과 대화 보존

Turn은 한 번의 사용자 요청, 그 과정의 도구 요청·결과, 최종 답변을 묶은 기록이다. Memory는 오래 기억할 사용자 정보이고, Summary는 오래된 대화의 압축본이다. 서로 다른 데이터다.

```text
모델 호출 직전, Context 크기를 코드로 확인
  ├─ 입력 예산의 약 80% 미만 → 그대로 모델 호출
  └─ 약 80% 이상
       ↓
    오래된 대화를 줄이기 전에,
    아직 Memory에 반영되지 않은 이전 대화에서
    오래 기억할 내용이 있는지 5. Memory 관리 흐름으로 확인·갱신
       ↓
    완료된 Turns를 두 그룹으로 나눔
      · 남김: 가장 최신 Turn은 반드시 유지하고,
              그 앞의 최근 Turns는 Context 한도의 12.5%에
              들어가는 만큼 유지 (고정된 개수가 아님)
      · 요약: 나머지 오래된 Turns
       ↓
    기존 Summary + 요약할 Turns → LLM이 새 Summary 생성
       ↓
    새 Summary가 비어 있지 않고 원본보다 짧은지 확인
       ↓
    새 Summary를 DB에 저장
      · 성공: 기존 Summary 하나를 교체한 뒤,
              새 Summary에 포함된 원본 Turns를 DB에서 삭제
      · 실패: 기존 Summary와 원본 Turns를 그대로 유지
       ↓
    새 Summary + 남긴 Turns + 이번 요청·도구 결과로
    Context를 다시 구성하고 크기를 재확인
      · 들어감: 모델 호출
      · 여전히 초과: 이번 요청 중단
```

Summary는 세션마다 현재본 **하나**를 저장한다. 다음 Compaction은 이전 Summary와 새로 오래어진 Turns를 합쳐 새 Summary로 **교체**하며, 과거 Summary 버전을 쌓지 않는다. 요약에 포함된 원본 Turns는 새 Summary 저장 성공 후 삭제되므로, Summary에 담기지 않은 오래된 세부사항은 복원할 수 없다.

최근 Turns는 12.5%라는 크기 몫으로 선택하므로 남는 **개수는 일정하지 않다**. 요약되지 않은 Turn은 PIA가 정한 보존기간(기본 30일)의 적용을 받고, 요약된 Turn은 그보다 일찍 삭제된다. 따라서 DB에는 장기 Memory, 세션의 현재 Summary, 남겨 둔 원본 Turns가 주로 남으며 **전체 대화 원문 이력을 보관하는 구조가 아니다**. 장기 Memory는 Compaction이 직접 삭제하지 않는다. 이번 요청과 아직 끝나지 않은 도구 사용은 오래된 완료 Turns를 고르는 대상이 아니다.

## 5. 장기 Memory 관리

Turn은 한 번의 대화 기록이고 Summary는 오래된 대화의 요약이다. **Memory는 이후 대화에 오래 도움이 될 사용자 정보를 담는 별도 문서**로, 사용자마다 현재본 하나만 두며 최대 4,000자다. 내용을 바꿀 때는 문서 전체를 교체하고 과거 버전을 쌓지 않는다.

```text
Memory 검토가 시작되는 세 경우
  ├─ 사용자가 “기억해줘 / 일부 잊어줘”라고 요청
  │    → 최종 answer를 고르는 Jev가 UPDATE / FORGET 선택
  │    → 방금 받은 메시지도 Memory 작성에 포함
  ├─ 반영 여부를 확인하지 않은 완료 대화가 남아 있고,
  │   그중 가장 최근 대화로부터 12시간 이상 지난 뒤
  │   사용자가 다시 메시지를 보냄
  │    → 별도 타이머 없이 그 요청을 처리할 때 검토
  └─ Compaction 직전
       → 반영 여부를 확인하지 않은 완료 대화가 있으면 검토
                    ↓
          변경이 필요할 때 같은 Memory 작성으로 이어짐
                    ↓
       PIA의 금융 투자용 Memory 지침
       + 현재 Memory 문서 + 검토할 대화
                    ↓
       ◇ 명시적 MemoryAction인가? — 코드의 if문
         ├─ UPDATE / FORGET → LLM이 새 Memory 문서 전체 작성
         └─ 자동 검토 → ◇ Jev AI가 변경 필요 여부 판단
                       ├─ 불필요 → Memory 문구 유지
                       └─ 필요 → LLM이 새 Memory 문서 전체 작성
       → 새 문서를 작성한 경우에만 최대 4,000자인지 검증 후 교체
                    ↓
       마지막으로 검토한 대화 위치를 함께 저장
       → 같은 대화를 반복 검토하지 않음
```

PIA는 “금융 투자 에이전트가 장기적으로 기억할 내용”을 정한 Memory 지침을 Harness에 반드시 전달한다. 범용 Harness에는 금융 투자용 지침을 고정하지 않는다. Jev는 `answer`를 선택할 때 이번 사용자 요청만 근거로 `NONE / UPDATE / FORGET`을 함께 결정하며 별도 Jev 호출을 추가하지 않는다. 일반 답변 작성과 Memory 문서 작성은 별도 LLM 호출이다. 자동 검토에서 변경 여부는 Jev AI가 판단하고, 변경이 필요할 때만 LLM이 새 문서를 작성한다.

의미 있는 Memory 변경은 별도 알림 대신 일반 답변 끝에 최대 3개(각 200자 이하) 안내한다. 자동 검토가 실패하면 조용히 넘어가고, 사용자가 직접 요청한 기억·잊기가 실패하면 답변에서 알린다.

`FORGET`은 사용자가 지정한 내용을 지우는 기능이다. 지정한 내용이 Memory의 마지막 항목이면 문서가 비워질 수 있다. 대화 문맥 전체를 초기화하는 별도 기능은 제공하지 않는다.

## 6. 도구 사양

1번에서 Jev가 선택하는 구체적 도구의 동작을 이곳에 모은다. 도구는 Harness가 직접 호출하며, 받은 결과를 이번 Turn의 Context에 추가한 뒤 1번의 Jev 판단으로 돌아간다. **답변만 종료 지점**이다.

### 6.1 읽기 도구

읽기 도구는 외부 자료나 현재 상태를 가져오며, 그 자체로 주문 같은 상태 변경을 하지 않는다.

Jev가 현재 Context로 사용자 질문에 충분히 답할 수 있다고 판단하면 `answer`를 선택한다. 기사 수나 "자료 충분" 상태 같은 별도 규칙은 두지 않는다. 답변 LLM은 이번 Turn에서 **본문을 읽은 자료만** 내용의 근거와 출처로 사용한다. 본문을 읽지 않은 후보는 Jev의 판단 자료일 뿐이며, 읽지 못한 내용을 읽은 것처럼 쓰지 않는다. 검색 결과와 본문은 모델을 지휘하는 지침이 아니라 참고 자료다.

사용자 요청, 검색어·후보, 읽은 본문, 최종 답변은 3번의 완료 Turn 규칙에 따라 그대로 저장한다. 본문 보관에 따른 토큰·Compaction 부담은 실제 사용에서 확인한 뒤 필요하면 보관 범위를 다시 정한다. 반복 탐색은 1번의 읽기 라우팅 시간 상한(기본 120초)이 제한한다.

OpenRouter 내장 Web Search는 사용하지 않는다. 검색은 `search` 도구 한 경로로만 한다.

#### 6.1.1 Search

Search는 뉴스 기사 후보를 찾는 읽기 도구다. 첫 공급자는 NAVER API HUB 뉴스 검색 API다. 검색 한 번에 기사 후보를 최대 5건 받으며, 후보에는 기사 본문이 없다. 본문이 필요하면 Jev가 6.1.2 본문 읽기를 따로 선택한다.

```text
◇ Jev가 search 선택
    ↓
LLM이 검색어와 정렬을 한 줄로 작성
  · 키워드 2~5개, 지시어는 대상 이름으로 바꿈
  · 이미 한 검색과 다른 각도
  · 최신 동향을 묻는 질문이면 date, 그 외 sim
    ↓
Harness가 네이버 뉴스 검색 API를 1회 호출 (display=5)
    ↓
기사 후보 최대 5건
  · 제목·짧은 설명·링크·날짜 (본문 없음)
  · 이번 Turn에서 이미 받은 링크는 제외
  · 새 후보마다 ID 부여 (c1, c2, …)
    ↓
검색어와 후보를 이번 Turn의 Context에 추가
    ↓
◇ Jev가 다시 선택
  ├─ 더 찾을 필요가 있음 → search (다른 검색어)
  ├─ 기사 본문 확인이 필요함 → read:후보ID
  └─ 답변하기에 충분함 → answer + MemoryAction
```

Jev는 텍스트를 반환하지 않으므로 검색어는 `search`를 선택했을 때만 LLM이 작성한다. 다른 도구에는 이 인자 작성 단계를 강제하지 않는다. `sort=sim`은 정확도순, `sort=date`는 최신순이다. 후보의 날짜가 Context에 함께 들어가므로 오래된 기사만 모였으면 Jev가 `date` 정렬로 다시 검색하도록 고를 수 있다. 날짜 기준의 자동 제외 규칙은 두지 않는다. 한 Turn에서 `search`를 여러 번 선택할 수 있어 후보는 5건보다 많아질 수 있다.

#### 6.1.2 본문 읽기

본문 읽기는 URL 하나의 본문을 가져오는 범용 읽기 도구다. 대상은 이번 Turn의 검색 후보(`c1…`)와 이번 사용자 메시지에 포함된 URL(`u1…`)이다. Harness는 아직 읽지 않은 대상마다 `read:후보ID` 선택지를 만들어 Jev에게 보여준다. Jev가 선택지 하나를 고르는 것으로 읽을 대상이 정해지므로 별도의 인자 작성 LLM 호출이나 필터 도구가 없다. 사용자가 URL을 준 경우에는 Search 없이 처음부터 본문 읽기를 고를 수 있다.

```text
◇ Jev가 read:후보ID 하나 선택
    ↓
Harness가 해당 URL 본문 1건 읽기
  · 네이버 뉴스 링크가 있으면 그 링크, 없으면 원문 링크
  · 성공: 본문 (기사당 약 6,000자까지)
  · 실패: 읽지 못했다는 사실만
    ↓
결과를 이번 Turn의 Context에 추가
  · 읽은 후보는 read: 선택지에서 제외
    ↓
◇ Jev가 다시 선택 (answer · search · 남은 read:후보ID)
```

URL 접근은 최소한으로만 검증한다. http·https만 허용하고, 사설·loopback·link-local 주소(EC2 metadata 포함)는 거부하며, redirect마다 같은 검사를 반복한다. 요청 타임아웃과 다운로드 크기 상한을 둔다.

## 기타: 지원 계약

아래 항목은 위 대화 흐름을 새로 나누지 않는 기본 계약이다.

- Harness는 모델 응답을 검증하고 실패 단계를 구분해 PIA에 알린다. 모델 요청의 재시도는 제한적이며, 실패한 도구를 Harness가 자동으로 다시 실행하지 않는다.
- Jev가 다음 행동을 고르지 못하면 `answer + NONE`으로 우회하지 않고 실패 상태를 PIA에 전달한다.
- Harness는 등록된 도구의 입력을 확인하고 도구 API를 직접 호출한다. pia-broker는 계좌 소유권·주문 한도·중복 방지 등 거래 규칙을 최종 검증한다. 알려진 실패나 결과 미확정도 Harness가 도구 결과로 다뤄 Jev의 다음 판단에 제공한다.
- 저장소는 사용자별 데이터 분리, Turn 순서·만료, 조건부 갱신을 지킨다. 회원 상태에 따른 대화 중단과 계정 데이터 삭제는 PIA가 맡는다.
- 진행 중이라는 안내와 오류 문구를 사용자 채널에 어떻게 보여줄지는 PIA가 정한다. Harness는 필요한 결과 상태와 선택적 진행 신호만 전달한다.
